<!DOCTYPE html>
<html lang="ar">

<head>
  <meta charset="UTF-8" />
  <title>حالة الحجز</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark py-2" dir="rtl">
        <div class="container d-flex flex-column flex-lg-row align-items-center justify-content-between">
            <!-- Right on large screens, top on mobile -->
            <span class="navbar-text fs-5 fw-bold">
                رئاسة جامعة كركوك - مركز الحاسبة
            </span>

            <!-- Left on large screens, bottom on mobile -->
            <a class="navbar-brand fw-bold fs-4" href="/">
                تطبيق الحجز
            </a>
        </div>
    </nav>

  <div class="container py-4">
    <!-- Message -->
    <div class="alert alert-<%= messageType %>">
      <%= message %>
    </div>

    <!-- Student info -->
    <div class="card shadow-sm mb-3">
      <div class="card-header fw-bold">معلومات الطالب</div>
      <div class="card-body">
        <div class="row gy-2">
          <div class="col-md-4"><strong>الاسم:</strong>
            <%= student.name %>
          </div>
          <div class="col-md-4"><strong>رقم الهاتف:</strong>
            <%= student.phone_number %>
          </div>
          <div class="col-md-4"><strong>رقم الهوية:</strong>
            <%= student.id_number %>
          </div>
        </div>
      </div>
    </div>

    <!-- Reservation details -->
    <div class="card shadow-sm mb-4">
      <div class="card-header fw-bold">تفاصيل الحجز</div>
      <div class="card-body">
        <div class="row gy-2">
          <div class="col-md-3"><strong>التاريخ:</strong>
            <%= reservation.date %>
          </div>
          <div class="col-md-3"><strong>المركز:</strong>
            <%= reservation.center_name %>
          </div>
          <div class="col-md-3"><strong>رقم الحجز:</strong>
            <%= reservation.id %>
          </div>
          <div class="col-md-3"><strong>السعة:</strong>
            <%= reservation.capacity %>
          </div>
          <div class="col-md-3"><strong>المحجوز:</strong>
            <%= reservation.reserved %>
          </div>
          <div class="col-md-3"><strong>المتاحة:</strong>
            <%= reservation.available %>
          </div>
        </div>
      </div>
    </div>

    <!-- Back button -->
    <a href="/students/new" class="btn btn-secondary">← العودة لإنشاء حجز جديد</a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>